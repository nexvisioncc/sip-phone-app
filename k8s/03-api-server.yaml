apiVersion: apps/v1
kind: Deployment
metadata:
  name: sip-api
  namespace: sip-phone
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sip-api
  template:
    metadata:
      labels:
        app: sip-api
    spec:
      containers:
      - name: api
        image: node:20-alpine
        workingDir: /app
        command:
        - sh
        - -c
        - |
          echo 'Starting SIP API server...'
          cat > package.json << 'EOF'
          {
            "name": "sip-api",
            "version": "1.0.0",
            "dependencies": {
              "express": "^4.18.2",
              "ws": "^8.13.0",
              "firebase-admin": "^12.0.0"
            }
          }
          EOF
          npm install
          cat > server.js << 'EOF'
          'use strict';
          const express  = require('express');
          const http     = require('http');
          const WebSocket = require('ws');
          const crypto   = require('crypto');
          const admin    = require('firebase-admin');

          const app    = express();
          app.use(express.json());
          const server = http.createServer(app);

          // WebSocket server for Flutter clients — path /ws
          const wss = new WebSocket.Server({ server, path: '/ws' });

          // ── Firebase ─────────────────────────────────────────────────────
          let firebaseReady = false;
          try {
            const sa = require(process.env.GOOGLE_APPLICATION_CREDENTIALS);
            admin.initializeApp({ credential: admin.credential.cert(sa) });
            firebaseReady = true;
            console.log('[Firebase] initialized');
          } catch (e) {
            console.warn('[Firebase] skipped:', e.message);
          }

          // ── Config ───────────────────────────────────────────────────────
          const ASTERISK_WS = 'ws://asterisk:8080/ws';
          const SIP_USER    = 'herberttung';
          const SIP_PASS    = '87828Idiot';
          const SIP_DOMAIN  = 'sip.nexvision.cc';

          // ── State ────────────────────────────────────────────────────────
          let sipWs         = null;
          let sipRegistered = false;
          let flutterWs     = null;   // single active Flutter client
          let deviceToken   = null;   // FCM token for push when WS is closed

          let regCallId     = null;
          let regCseq       = 1;
          let regFromTag    = null;
          let regTimer      = null;

          // Call dialog state
          let pendingInvite     = null;  // incoming call ringing: { via,from,to,callId,cseq,contact,body }
          let outgoingInvite    = null;  // outgoing INVITE in-flight: { callId,fromTag,toUri,branch,sdpBody,cseq }
          let activeDialog      = null;  // active call (incoming or outgoing): { callId,localTag,remoteTag,remoteUri,fromHeader,outCseq }
          let activeCallSdpAnswer = null; // SDP answer from Asterisk for active outgoing call; re-sent on Flutter reconnect
          let cancelledCallId     = null; // call_id of an outgoing CANCEL we sent; used to ACK+BYE any late 200 OK

          // ── Helpers ──────────────────────────────────────────────────────
          function uuid() { return crypto.randomBytes(8).toString('hex'); }

          function md5(s) { return crypto.createHash('md5').update(s).digest('hex'); }

          // Compute SIP Digest Authorization header value
          function digestAuth(method, uri, wwwAuth) {
            const realmM = wwwAuth.match(/realm="([^"]+)"/);
            const nonceM = wwwAuth.match(/nonce="([^"]+)"/);
            if (!realmM || !nonceM) return null;
            const realm = realmM[1];
            const nonce = nonceM[1];
            const ha1 = md5(`${SIP_USER}:${realm}:${SIP_PASS}`);
            const ha2 = md5(`${method}:${uri}`);
            const response = md5(`${ha1}:${nonce}:${ha2}`);
            return `Digest username="${SIP_USER}", realm="${realm}", nonce="${nonce}", uri="${uri}", algorithm=MD5, response="${response}"`;
          }

          function getHeader(lines, name) {
            const lc = name.toLowerCase();
            for (const line of lines) {
              const ci = line.indexOf(':');
              if (ci > 0 && line.slice(0, ci).toLowerCase().trim() === lc)
                return line.slice(ci + 1).trim();
            }
            return '';
          }

          function parseSip(raw) {
            const di   = raw.indexOf('\r\n\r\n');
            const hdr  = di >= 0 ? raw.slice(0, di) : raw;
            const body = di >= 0 ? raw.slice(di + 4) : '';
            const lines = hdr.split('\r\n');
            return { first: lines[0], lines, body };
          }

          function buildResp(status, inv, extra = [], body = '') {
            return [
              `SIP/2.0 ${status}`,
              `Via: ${inv.via}`,
              `From: ${inv.from}`,
              `To: ${inv.to}`,
              `Call-ID: ${inv.callId}`,
              `CSeq: ${inv.cseq}`,
              ...extra,
              `Content-Length: ${Buffer.byteLength(body)}`,
              '', body,
            ].join('\r\n');
          }

          function sendSip(msg) {
            if (sipWs && sipWs.readyState === WebSocket.OPEN) sipWs.send(msg);
          }

          function sendFlutter(obj) {
            if (flutterWs && flutterWs.readyState === WebSocket.OPEN)
              flutterWs.send(JSON.stringify(obj));
          }

          // ── FCM Push ─────────────────────────────────────────────────────
          async function sendFcmPush(caller, callId) {
            if (!firebaseReady || !deviceToken) {
              console.warn('[FCM] Cannot push — ready:', firebaseReady, 'token:', !!deviceToken);
              return;
            }
            try {
              await admin.messaging().send({
                token: deviceToken,
                data: {
                  type: 'incoming_call',
                  call_id: callId,
                  caller_name: caller,
                  caller_number: caller,
                },
                android: { priority: 'high' },
              });
              console.log('[FCM] Incoming call push sent for', callId);
            } catch (e) {
              console.error('[FCM] Push failed:', e.message);
            }
          }

          // ── SIP Registration ─────────────────────────────────────────────
          function register() {
            if (!regCallId) { regCallId = uuid(); regFromTag = uuid(); }
            const branch = 'z9hG4bK' + uuid();
            sendSip([
              `REGISTER sip:${SIP_DOMAIN} SIP/2.0`,
              `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${branch};rport`,
              `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${regFromTag}`,
              `To: <sip:${SIP_USER}@${SIP_DOMAIN}>`,
              `Call-ID: ${regCallId}`,
              `CSeq: ${regCseq++} REGISTER`,
              `Contact: <sip:${SIP_USER}@${SIP_DOMAIN};transport=ws>`,
              `Expires: 3600`,
              `Content-Length: 0`,
              '', '',
            ].join('\r\n'));
          }

          // ── SIP Message Router ───────────────────────────────────────────
          function handleSipMsg(raw) {
            const { first, lines, body } = parseSip(raw);

            if (first.startsWith('SIP/2.0')) {
              const code    = parseInt(first.split(' ')[1]);
              const cseqHdr = getHeader(lines, 'CSeq');
              const method  = cseqHdr.split(' ').pop();

              if (method === 'REGISTER' && code === 401) {
                // Digest auth challenge — re-register with credentials
                const wwwAuth = getHeader(lines, 'WWW-Authenticate');
                console.log('[SIP] 401 challenge, re-registering with digest auth');
                const authHeader = digestAuth('REGISTER', `sip:${SIP_DOMAIN}`, wwwAuth);
                if (!authHeader) { console.error('[SIP] Failed to compute digest'); return; }
                const branch = 'z9hG4bK' + uuid();
                sendSip([
                  `REGISTER sip:${SIP_DOMAIN} SIP/2.0`,
                  `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${branch};rport`,
                  `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${regFromTag}`,
                  `To: <sip:${SIP_USER}@${SIP_DOMAIN}>`,
                  `Call-ID: ${regCallId}`,
                  `CSeq: ${regCseq++} REGISTER`,
                  `Contact: <sip:${SIP_USER}@${SIP_DOMAIN};transport=ws>`,
                  `Expires: 3600`,
                  `Authorization: ${authHeader}`,
                  `Content-Length: 0`,
                  '', '',
                ].join('\r\n'));
                return;
              }
              if (method === 'REGISTER' && code === 200) {
                console.log('[SIP] Registered with Asterisk');
                sipRegistered = true;
                sendFlutter({ type: 'registered' });
                if (regTimer) clearTimeout(regTimer);
                regTimer = setTimeout(register, 3500 * 1000);
              }

              // ── Late 200 OK after we already sent CANCEL ──────────────────
              // Race: CANCEL crossed with Asterisk's 200 OK — must ACK then BYE
              if (method === 'INVITE' && code === 200 && !outgoingInvite && cancelledCallId) {
                const oid = getHeader(lines, 'Call-ID');
                if (oid === cancelledCallId) {
                  const toHdr      = getHeader(lines, 'To');
                  const contactHdr = getHeader(lines, 'Contact');
                  const remoteUriM = contactHdr.match(/<([^>]+)>/);
                  const remoteUri  = remoteUriM ? remoteUriM[1] : '';
                  const fromTagM   = getHeader(lines, 'From').match(/;tag=([^\s;>]+)/);
                  const fromTag    = fromTagM ? fromTagM[1] : '';
                  const ackBranch  = 'z9hG4bK' + uuid();
                  const byeBranch  = 'z9hG4bK' + uuid();
                  // ACK the 200 OK
                  sendSip([
                    `ACK ${remoteUri} SIP/2.0`,
                    `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${ackBranch};rport`,
                    `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${fromTag}`,
                    `To: ${toHdr}`,
                    `Call-ID: ${oid}`,
                    `CSeq: 2 ACK`,
                    'Content-Length: 0',
                    '', '',
                  ].join('\r\n'));
                  // Immediately BYE to tear down the PSTN leg
                  sendSip([
                    `BYE ${remoteUri} SIP/2.0`,
                    `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${byeBranch};rport`,
                    `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${fromTag}`,
                    `To: ${toHdr}`,
                    `Call-ID: ${oid}`,
                    `CSeq: 3 BYE`,
                    'Content-Length: 0',
                    '', '',
                  ].join('\r\n'));
                  cancelledCallId = null;
                  console.log('[SIP] Late 200 OK after CANCEL — ACK+BYE sent', oid);
                }
              }

              // ── Outgoing INVITE responses ─────────────────────────────────
              if (method === 'INVITE' && outgoingInvite) {
                const oid = getHeader(lines, 'Call-ID');
                if (oid === outgoingInvite.callId) {
                  if (code === 100) { /* ignore */ }
                  else if (code === 401 || code === 407) {
                    // Asterisk challenges the INVITE — send ACK then re-INVITE with auth
                    const toHdr = getHeader(lines, 'To');
                    const ackBranch = 'z9hG4bK' + uuid();
                    sendSip([
                      `ACK ${outgoingInvite.toUri} SIP/2.0`,
                      `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${ackBranch};rport`,
                      `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${outgoingInvite.fromTag}`,
                      `To: ${toHdr}`,
                      `Call-ID: ${oid}`,
                      `CSeq: ${outgoingInvite.cseq} ACK`,
                      'Content-Length: 0',
                      '', '',
                    ].join('\r\n'));

                    const authHdr = code === 407
                      ? getHeader(lines, 'Proxy-Authenticate')
                      : getHeader(lines, 'WWW-Authenticate');
                    const authValue = digestAuth('INVITE', outgoingInvite.toUri, authHdr);
                    if (!authValue) {
                      console.error('[SIP] Failed to compute INVITE digest auth');
                      sendFlutter({ type: 'call_state', call_id: oid, state: 'failed' });
                      outgoingInvite = null;
                      return;
                    }
                    outgoingInvite.cseq += 1;
                    console.log('[SIP] Outgoing INVITE auth challenge — re-sending with credentials');
                    sendInvite(outgoingInvite.toUri, outgoingInvite.fromTag, oid,
                               outgoingInvite.cseq, outgoingInvite.sdpBody, authValue);
                  } else if (code >= 180 && code < 200) {
                    console.log('[SIP] Outgoing call ringing', oid);
                    if (body && body.trim()) logSdpCandidates('ANSWER-183-from-Asterisk', body);
                    const earlyMsg = { type: 'call_state', call_id: oid, state: 'ringing' };
                    if (body && body.trim()) earlyMsg.sdp_answer = body;
                    sendFlutter(earlyMsg);
                  } else if (code === 200) {
                    const toHdr     = getHeader(lines, 'To');
                    const contactHdr = getHeader(lines, 'Contact');
                    const remoteUriM = contactHdr.match(/<([^>]+)>/);
                    const remoteUri  = remoteUriM ? remoteUriM[1] : outgoingInvite.toUri;
                    const sdpAnswer  = body;
                    logSdpCandidates('ANSWER-200-from-Asterisk', sdpAnswer);

                    // Send ACK
                    const ackBranch = 'z9hG4bK' + uuid();
                    sendSip([
                      `ACK ${remoteUri} SIP/2.0`,
                      `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${ackBranch};rport`,
                      `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${outgoingInvite.fromTag}`,
                      `To: ${toHdr}`,
                      `Call-ID: ${oid}`,
                      `CSeq: ${outgoingInvite.cseq} ACK`,
                      'Content-Length: 0',
                      '', '',
                    ].join('\r\n'));

                    // Store dialog for BYE / DTMF
                    // outCseq must be > last INVITE CSeq (which may be 2 after auth challenge)
                    activeDialog = {
                      callId:     oid,
                      localTag:   outgoingInvite.fromTag,
                      remoteTag:  (toHdr.match(/;tag=([^\s;>]+)/) || [])[1] || '',
                      remoteUri,
                      fromHeader: toHdr,   // To: from our INVITE (with Asterisk's tag) → used as To: in BYE
                      outCseq:    outgoingInvite.cseq + 1,
                    };
                    activeCallSdpAnswer = sdpAnswer; // stored for re-sync if Flutter reconnects
                    outgoingInvite = null;
                    console.log('[SIP] Outgoing call connected', oid);
                    sendFlutter({ type: 'call_state', call_id: oid, state: 'active', sdp_answer: sdpAnswer });
                  } else if (code >= 400) {
                    console.log('[SIP] Outgoing call failed:', code, oid);
                    // 487 = CANCEL accepted — don't re-send 'ended' (already sent in hangupCall)
                    if (code !== 487) sendFlutter({ type: 'call_state', call_id: oid, state: 'failed' });
                    if (cancelledCallId === oid) cancelledCallId = null; // 487 = no late 200 OK coming
                    outgoingInvite = null;
                  }
                }
              }

              return;
            }

            const method = first.split(' ')[0];
            switch (method) {
              case 'INVITE':  handleInvite(lines, body); break;
              case 'ACK':     handleAck(lines);          break;
              case 'BYE':     handleBye(lines);          break;
              case 'CANCEL':  handleCancel(lines);       break;
              case 'OPTIONS':
                sendSip([
                  'SIP/2.0 200 OK',
                  `Via: ${getHeader(lines, 'Via')}`,
                  `From: ${getHeader(lines, 'From')}`,
                  `To: ${getHeader(lines, 'To')}`,
                  `Call-ID: ${getHeader(lines, 'Call-ID')}`,
                  `CSeq: ${getHeader(lines, 'CSeq')}`,
                  'Content-Length: 0', '', '',
                ].join('\r\n'));
                break;
            }
          }

          // ── INVITE ───────────────────────────────────────────────────────
          function handleInvite(lines, body) {
            const callId = getHeader(lines, 'Call-ID');

            // Retransmission: re-send 180 Ringing
            if (pendingInvite && pendingInvite.callId === callId) {
              sendSip(buildResp('180 Ringing', pendingInvite,
                [`Contact: <sip:${SIP_USER}@${SIP_DOMAIN};transport=ws>`]));
              return;
            }

            const via    = getHeader(lines, 'Via');
            const from   = getHeader(lines, 'From');
            const toRaw  = getHeader(lines, 'To');
            const cseq   = getHeader(lines, 'CSeq');
            const contact = getHeader(lines, 'Contact');
            const to     = `${toRaw};tag=${uuid()}`;

            pendingInvite = { via, from, to, callId, cseq, contact, body };

            sendSip(buildResp('100 Trying', pendingInvite));
            sendSip(buildResp('180 Ringing', pendingInvite,
              [`Contact: <sip:${SIP_USER}@${SIP_DOMAIN};transport=ws>`]));

            const callerMatch = from.match(/sip:([^@>]+)/);
            const caller = callerMatch ? callerMatch[1] : 'Unknown';
            console.log('[SIP] Incoming call from', caller, '— call-id:', callId);

            sendFlutter({ type: 'incoming_call', call_id: callId, from: caller, sdp_offer: body });
            // Send FCM push so backgrounded/killed app can show incoming call UI
            sendFcmPush(caller, callId);

            // Auto-reject after 60 s if Flutter doesn't answer (extra time for FCM delivery)
            setTimeout(() => {
              if (pendingInvite && pendingInvite.callId === callId) {
                console.log('[SIP] Timeout: rejecting unanswered call', callId);
                sendSip(buildResp('486 Busy Here', pendingInvite));
                sendFlutter({ type: 'call_state', call_id: callId, state: 'ended' });
                pendingInvite = null;
              }
            }, 60000);
          }

          // ── ACK (call connected) ─────────────────────────────────────────
          function handleAck(lines) {
            const callId = getHeader(lines, 'Call-ID');
            console.log('[SIP] ACK — call active', callId);
            if (activeDialog && activeDialog.callId === callId) {
              sendFlutter({ type: 'call_state', call_id: callId, state: 'active' });
            }
          }

          // ── BYE ──────────────────────────────────────────────────────────
          function handleBye(lines) {
            const callId = getHeader(lines, 'Call-ID');
            const inv = {
              via:    getHeader(lines, 'Via'),
              from:   getHeader(lines, 'From'),
              to:     getHeader(lines, 'To'),
              callId,
              cseq:   getHeader(lines, 'CSeq'),
            };
            sendSip(buildResp('200 OK', inv));
            activeDialog      = null;
            pendingInvite     = null;
            activeCallSdpAnswer = null;
            sendFlutter({ type: 'call_state', call_id: callId, state: 'ended' });
            console.log('[SIP] BYE — call ended', callId);
          }

          // ── CANCEL ───────────────────────────────────────────────────────
          function handleCancel(lines) {
            const callId = getHeader(lines, 'Call-ID');
            const inv = {
              via:    getHeader(lines, 'Via'),
              from:   getHeader(lines, 'From'),
              to:     getHeader(lines, 'To'),
              callId,
              cseq:   getHeader(lines, 'CSeq'),
            };
            sendSip(buildResp('200 OK', inv));
            if (pendingInvite && pendingInvite.callId === callId) {
              sendSip(buildResp('487 Request Terminated', pendingInvite));
              pendingInvite = null;
            }
            sendFlutter({ type: 'call_state', call_id: callId, state: 'ended' });
            console.log('[SIP] CANCEL — call ended', callId);
          }

          // ── Answer / Reject / Hangup / DTMF ─────────────────────────────
          function answerCall(callId, sdpAnswer) {
            if (!pendingInvite || pendingInvite.callId !== callId) {
              console.warn('[SIP] answerCall: no pending invite for', callId);
              return;
            }
            const inv = pendingInvite;
            sendSip(buildResp('200 OK', inv, [
              `Contact: <sip:${SIP_USER}@${SIP_DOMAIN};transport=ws>`,
              'Content-Type: application/sdp',
            ], sdpAnswer));

            // Store dialog state for outgoing BYE / INFO
            const remoteTagM  = inv.from.match(/;tag=([^\s;>]+)/);
            const contactUriM = inv.contact.match(/<([^>]+)>/);
            activeDialog = {
              callId,
              localTag:   inv.to.match(/;tag=([^\s;>]+)/)?.[1] || '',
              remoteTag:  remoteTagM  ? remoteTagM[1]  : '',
              remoteUri:  contactUriM ? contactUriM[1] : (inv.from.match(/<([^>]+)>/)?.[1] || ''),
              fromHeader: inv.from,
              outCseq:    2,
            };
            pendingInvite = null;
            console.log('[SIP] 200 OK sent for call', callId);
          }

          function rejectCall(callId) {
            if (!pendingInvite || pendingInvite.callId !== callId) return;
            sendSip(buildResp('486 Busy Here', pendingInvite));
            sendFlutter({ type: 'call_state', call_id: callId, state: 'ended' });
            pendingInvite = null;
            console.log('[SIP] Rejected call', callId);
          }

          function hangupCall(callId) {
            // Outgoing call: still dialing/ringing — send CANCEL
            if (outgoingInvite && outgoingInvite.callId === callId) {
              const inv = outgoingInvite;
              outgoingInvite = null;
              const branch = inv.branch || ('z9hG4bK' + uuid());
              sendSip([
                `CANCEL ${inv.toUri} SIP/2.0`,
                `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${branch};rport`,
                `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${inv.fromTag}`,
                `To: <${inv.toUri}>`,
                `Call-ID: ${inv.callId}`,
                `CSeq: ${inv.cseq} CANCEL`,
                'Content-Length: 0',
                '', '',
              ].join('\r\n'));
              cancelledCallId = callId;  // remember in case late 200 OK arrives
              sendFlutter({ type: 'call_state', call_id: callId, state: 'ended' });
              console.log('[SIP] CANCEL sent for outgoing call', callId);
              return;
            }
            if (activeDialog && activeDialog.callId === callId) {
              const d = activeDialog;
              const branch = 'z9hG4bK' + uuid();
              sendSip([
                `BYE ${d.remoteUri} SIP/2.0`,
                `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${branch};rport`,
                `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${d.localTag}`,
                `To: ${d.fromHeader}`,
                `Call-ID: ${d.callId}`,
                `CSeq: ${d.outCseq++} BYE`,
                'Content-Length: 0',
                '', '',
              ].join('\r\n'));
              activeDialog      = null;
              activeCallSdpAnswer = null;
              console.log('[SIP] BYE sent for call', callId);
              return;
            }
            // Force-terminate if still ringing (ACK not yet received)
            if (pendingInvite && pendingInvite.callId === callId) {
              sendSip(buildResp('486 Busy Here', pendingInvite));
              sendFlutter({ type: 'call_state', call_id: callId, state: 'ended' });
              pendingInvite = null;
              console.log('[SIP] Force-rejected pending call', callId);
              return;
            }
            console.warn('[SIP] hangupCall: no active or pending call for', callId);
          }

          // ── Outgoing call (Flutter → PSTN via Asterisk + Twilio trunk) ──────
          function sendInvite(toUri, fromTag, callId, cseq, sdpBody, authHeader) {
            const branch = 'z9hG4bK' + uuid();
            if (outgoingInvite) outgoingInvite.branch = branch; // store for CANCEL
            const lines = [
              `INVITE ${toUri} SIP/2.0`,
              `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${branch};rport`,
              `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${fromTag}`,
              `To: <${toUri}>`,
              `Call-ID: ${callId}`,
              `CSeq: ${cseq} INVITE`,
              `Contact: <sip:${SIP_USER}@${SIP_DOMAIN};transport=ws>`,
              `Max-Forwards: 70`,
            ];
            if (authHeader) lines.push(`Authorization: ${authHeader}`);
            lines.push('Content-Type: application/sdp');
            lines.push(`Content-Length: ${Buffer.byteLength(sdpBody)}`);
            lines.push('', sdpBody);
            sendSip(lines.join('\r\n'));
          }

          function logSdpCandidates(label, sdp) {
            if (!sdp) return;
            const lines = sdp.split(/\r?\n/);
            const interesting = lines.filter(l =>
              l.startsWith('a=candidate:') || l.startsWith('a=ice-ufrag') ||
              l.startsWith('a=fingerprint') || l.startsWith('c=') || l.startsWith('m=') ||
              l.startsWith('a=setup:') || l.startsWith('a=sendrecv') || l.startsWith('a=sendonly'));
            console.log(`[SDP:${label}]`, interesting.slice(0, 30).join(' | '));
          }

          function initiateOutgoingCall(number, sdpOffer, callId) {
            if (!sipRegistered) {
              console.warn('[SIP] Cannot place outgoing call — not registered');
              sendFlutter({ type: 'call_state', call_id: callId, state: 'failed' });
              return;
            }
            const fromTag = uuid();
            const toUri   = `sip:${number}@${SIP_DOMAIN}`;
            const body    = sdpOffer || '';
            const cseq    = 1;

            outgoingInvite = { callId, fromTag, toUri, sdpBody: body, cseq };

            logSdpCandidates('OFFER-from-Flutter', body);
            sendInvite(toUri, fromTag, callId, cseq, body, null);
            console.log('[SIP] Outgoing INVITE to', number, '— call-id:', callId);
            sendFlutter({ type: 'call_state', call_id: callId, state: 'dialing' });
          }

          function sendDtmf(callId, tone) {
            if (!activeDialog || activeDialog.callId !== callId) return;
            const d    = activeDialog;
            const branch = 'z9hG4bK' + uuid();
            const body = `Signal=${tone}\r\nDuration=160\r\n`;
            sendSip([
              `INFO ${d.remoteUri} SIP/2.0`,
              `Via: SIP/2.0/WS ${SIP_DOMAIN};branch=${branch};rport`,
              `From: <sip:${SIP_USER}@${SIP_DOMAIN}>;tag=${d.localTag}`,
              `To: ${d.fromHeader}`,
              `Call-ID: ${d.callId}`,
              `CSeq: ${d.outCseq++} INFO`,
              'Content-Type: application/dtmf-relay',
              `Content-Length: ${Buffer.byteLength(body)}`,
              '', body,
            ].join('\r\n'));
          }

          // ── SIP WebSocket (Asterisk) ──────────────────────────────────────
          // Keepalive: ping Asterisk every 20s to prevent HTTP session timeout
          setInterval(() => {
            if (sipWs && sipWs.readyState === WebSocket.OPEN) sipWs.ping();
          }, 20000);

          function connectSip() {
            sipWs = new WebSocket(ASTERISK_WS, ['sip']);

            sipWs.on('open', () => {
              console.log('[SIP] Connected to Asterisk');
              regCallId = null; regCseq = 1; regFromTag = null;
              register();
            });

            sipWs.on('message', (data) => handleSipMsg(data.toString()));

            sipWs.on('close', () => {
              sipRegistered = false;
              console.log('[SIP] Disconnected — reconnecting in 5s');
              if (regTimer) { clearTimeout(regTimer); regTimer = null; }
              sendFlutter({ type: 'error', message: 'server_disconnected' });
              setTimeout(connectSip, 5000);
            });

            sipWs.on('error', (e) => console.error('[SIP] error:', e.message));
          }

          // ── Flutter WebSocket Server ──────────────────────────────────────
          // Keepalive: ping Flutter clients every 25s to prevent proxy timeouts
          setInterval(() => {
            wss.clients.forEach(ws => {
              if (ws.isAlive === false) { ws.terminate(); return; }
              ws.isAlive = false;
              ws.ping();
            });
          }, 25000);

          wss.on('connection', (ws) => {
            console.log('[Flutter] Client connected');
            flutterWs = ws;
            ws.isAlive = true;
            ws.on('pong', () => { ws.isAlive = true; });

            // Immediately tell the client our current state
            if (sipRegistered) ws.send(JSON.stringify({ type: 'registered' }));

            // Re-sync any in-progress call the client missed due to WS drop
            if (pendingInvite) {
              const callerMatch = pendingInvite.from.match(/sip:([^@>]+)/);
              const caller = callerMatch ? callerMatch[1] : 'Unknown';
              ws.send(JSON.stringify({
                type: 'incoming_call',
                call_id: pendingInvite.callId,
                from: caller,
                sdp_offer: pendingInvite.body,
              }));
            } else if (outgoingInvite) {
              // Outgoing call still ringing — re-send ringing so Flutter updates UI
              ws.send(JSON.stringify({ type: 'call_state', call_id: outgoingInvite.callId, state: 'ringing' }));
            } else if (activeDialog) {
              // Active call — re-send active + SDP answer so Flutter can complete WebRTC ICE
              ws.send(JSON.stringify({ type: 'call_state', call_id: activeDialog.callId, state: 'active', sdp_answer: activeCallSdpAnswer || '' }));
            }

            ws.on('message', (data) => {
              let msg;
              try { msg = JSON.parse(data); } catch { return; }
              switch (msg.type) {
                case 'call':    initiateOutgoingCall(msg.number, msg.sdp_offer, msg.call_id); break;
                case 'answer':  answerCall(msg.call_id, msg.sdp_answer); break;
                case 'reject':  rejectCall(msg.call_id);                 break;
                case 'hangup':  hangupCall(msg.call_id);                 break;
                case 'dtmf':    sendDtmf(msg.call_id, msg.tone);         break;
              }
            });

            ws.on('close', () => {
              console.log('[Flutter] Client disconnected');
              if (flutterWs === ws) flutterWs = null;
            });
          });

          // ── REST Endpoints ────────────────────────────────────────────────
          // Test endpoint: simulates incoming call FCM push without needing real SIP INVITE
          app.post('/test-call', async (req, res) => {
            const caller = req.body.caller || 'TestCaller';
            const callId = 'test-' + uuid();
            console.log('[Test] Simulating incoming call from', caller, callId);
            // Minimal valid SDP offer so Flutter's WebRTC can process it
            const fakeSdp = [
              'v=0',
              'o=- 0 0 IN IP4 127.0.0.1',
              's=-',
              't=0 0',
              'm=audio 0 RTP/AVP 0',
              'a=sendrecv',
            ].join('\r\n') + '\r\n';
            const wsOpen = flutterWs?.readyState === WebSocket.OPEN;
            console.log('[Test] Flutter WS open:', wsOpen, '— sending incoming_call WS');
            sendFlutter({ type: 'incoming_call', call_id: callId, from: caller, sdp_offer: fakeSdp });
            await sendFcmPush(caller, callId);
            res.json({ success: true, call_id: callId, token: !!deviceToken });
          });

          app.get('/health', (_, res) => res.json({
            status: 'ok',
            sip_connected:    sipWs?.readyState === WebSocket.OPEN,
            sip_registered:   sipRegistered,
            flutter_connected: flutterWs?.readyState === WebSocket.OPEN,
            firebase:         firebaseReady,
          }));

          app.post('/devices', (req, res) => {
            const { fcm_token, platform } = req.body;
            console.log('Device registered:', { token: fcm_token?.slice(0, 20) + '...', platform });
            if (fcm_token) deviceToken = fcm_token;
            res.json({ success: true });
          });

          app.post('/push/send', async (req, res) => {
            try {
              if (!firebaseReady) return res.status(503).json({ error: 'Firebase not initialized' });
              const { token, title, body, data } = req.body;
              const resp = await admin.messaging().send({
                token,
                notification: { title, body },
                data: data || {},
                android: { priority: 'high', notification: { channelId: 'sip_calls_channel' } },
              });
              res.json({ success: true, messageId: resp });
            } catch (e) {
              res.status(500).json({ error: e.message });
            }
          });

          app.get('/users/:userId/sip-credentials', (req, res) => {
            res.json({ username: SIP_USER, domain: SIP_DOMAIN });
          });

          // ── Start ─────────────────────────────────────────────────────────
          const PORT = process.env.PORT || 3000;
          server.listen(PORT, '0.0.0.0', () => {
            console.log(`[Server] Listening on port ${PORT}`);
            connectSip();
          });
          EOF
          node server.js
        ports:
        - containerPort: 3000
        env:
        - name: PORT
          value: "3000"
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: /secrets/firebase-service-account.json
        volumeMounts:
        - name: firebase-secret
          mountPath: /secrets
          readOnly: true
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
      volumes:
      - name: firebase-secret
        secret:
          secretName: firebase-service-account
---
apiVersion: v1
kind: Service
metadata:
  name: sip-api
  namespace: sip-phone
spec:
  selector:
    app: sip-api
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP
